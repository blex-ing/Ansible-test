---
dependency:
  name: galaxy
# driver:
#   name: ansible #docker
platforms:
  - name: instance
    image: /usr/bin/docker #docker.io/pycontribs/centos:7
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
